import IconButton from "../UI/iconButton";

const CartItem = () => {
    return <div className="cart-item">
        <IconButton/>
        <p>product</p>
        <IconButton/>
    </div>;
};

export default CartItem;
